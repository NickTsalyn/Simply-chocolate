const swiper = new Swiper('.swiper', {
  // Optional parameters
  loop: true,
  spaceBetween: 19,
  slidesPerView: 'auto',
  // If we need pagination
  pagination: {
    el: '.swiper-pagination',
    clickable: true,
  },
});
